engines:
  duplication:
    enabled: true
    config:
      languages:
        javascript
        php:
          mass_threshold: 85
  eslint:
    enabled: true
  fixme:
    enabled: true
  phpmd:
    enabled: true
    config:
      rulesets: "codesize,design,unusedcode"
ratings:
  paths:
  - "**.js"
  - "**.php"
exclude_paths:
- assets/
